# Neon Postgres — copie da dashboard do Neon (Settings → Connection string)
# Use a versão "pooled" para Vercel serverless
DATABASE_URL="postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require"
